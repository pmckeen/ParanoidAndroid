<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.nndevelopments.paranoidandroid.ControlActivity$PlaceholderFragment" >

    <!--  Creates textview, used as label for button -->
    <TextView
        android:id="@+id/screamingPhoneLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="14dp"
        android:layout_marginTop="17dp"
        android:text="@string/screaming_phone" />

    <!--  creates button for controlling service -->
    <ToggleButton
        android:id="@+id/screamingPhoneToggle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/screamingPhoneLabel"
        android:layout_alignBottom="@+id/screamingPhoneLabel"
        android:layout_alignParentLeft="true"
        android:onClick="onScreamingToggleClick"
        android:checked="false" />

</RelativeLayout>